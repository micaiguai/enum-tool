{
  "name": "enum-tool",
  "version": "1.0.2",
  "description": "enum tool",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "publish": "pnpm run build && np --no-tests"
  },
  "keywords": [
    "enum"
  ],
  "author": "micaiguai",
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  },
  "files": [
    "dist/*",
    "package.json"
  ]
}
